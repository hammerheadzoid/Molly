using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyFoodCube : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Object.Destroy(gameObject, 19.0f);

    }

    // Update is called once per frame
    void Update () {
		
	}
}
